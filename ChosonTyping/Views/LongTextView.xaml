<UserControl x:Class="ChosonTyping.Views.LongTextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:ChosonTyping.Views"
             Focusable="True">
    <DockPanel>
        <views:StatsBar x:Name="Stats" DockPanel.Dock="Bottom"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40,18,40,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Style="{StaticResource QuietButton}"
                            Content="← 글 고르기" HorizontalAlignment="Left" Click="Back_Click"/>
                    <TextBlock x:Name="TitleText" Grid.Column="1" FontSize="13" FontWeight="Bold"
                               Foreground="{DynamicResource Mid}" VerticalAlignment="Center"/>
                </Grid>

                <TextBlock x:Name="PrevLine" FontSize="15" Foreground="{DynamicResource Mid}"
                           HorizontalAlignment="Center" Margin="0,22,0,8" TextWrapping="Wrap"/>
                <TextBlock x:Name="TargetLine" FontSize="27" FontWeight="SemiBold"
                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                <TextBlock x:Name="NextLine" FontSize="15" Foreground="{DynamicResource Faint}"
                           HorizontalAlignment="Center" Margin="0,8,0,6" TextWrapping="Wrap"/>
                <TextBlock x:Name="HintText" FontSize="12.5" HorizontalAlignment="Center"
                           Foreground="{DynamicResource Faint}" Margin="0,0,0,18"/>

                <views:KeyboardControl x:Name="Kb"/>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
